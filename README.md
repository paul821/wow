# WOW — Workout of the Week
- Deploy on Vercel. Optional: add a cron to hit `/api/weekly` daily.
- UI toggles: Bodyweight-only, No running. Jump rope is globally banned.
- We respect robots.txt: we fetch pages once, extract minimal text, and link back.
